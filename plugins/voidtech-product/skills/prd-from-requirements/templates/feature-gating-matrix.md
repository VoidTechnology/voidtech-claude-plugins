# 功能开通矩阵

- **日期**：{YYYY-MM-DD}
- **状态**：Draft
- **深度**：{骨架级/验收级}
- **适用条件**：多租户、多版本或套餐制产品。单一租户且无版本差异的产品不需要本文件。

> 「哪类租户、哪档套餐能用哪个功能」在此只定义一次；模块 PRD 引用功能点 ID，不各自描述开通条件。注意分工：本矩阵管功能开通（租户/套餐维度），模块 PRD 第 8 节权限矩阵管角色操作（角色 × 增删改查维度），两者不能互相替代，也不能互相指望对方补全自己的空缺。

## 1. 权限链

> 用一行说明从租户到最终可见性的收敛顺序，例如：租户类型（功能全集）→ 套餐/版本档（实际开通 + 容量）→ 租户内角色（套餐范围内收敛）→ 各端可见性（已开通 ∩ 租户自身开关）。

{权限链}

## 2. 租户类型 × 功能全集

| 租户类型 | 功能全集 | 来源需求 |
|---|---|---|
| {类型} | {功能列表} | {REQ-ID} |

## 3. 功能点 × 套餐/版本档 × 端

> 验收级要求逐功能点成行，覆盖全部模块；骨架级允许先列代表性功能点，但必须在「深化任务清单」登记补全任务，不允许用「示例」二字长期替代。

| 功能点 ID | 所属模块 | 租户类型可见 | 套餐/版本档 | 端 | 容量约束 |
|---|---|---|---|---|---|
| {FP-ID} | {模块} | {类型} | {档位} | {端} | {约束} |

## 4. 关键规则

> 到期、降级、超容量、宽限期等规则若非原文，必须按推断标记规范标注，并登记进全局开放问题清单后在此回填编号；不允许只写「来源：开放问题」而无编号。

| 规则 | 默认方案 | 标注（原文/[推断]/[推荐默认]） | 对应开放问题编号 |
|---|---|---|---|
| {规则} | {方案} | {标注} | {OQ-编号或“不适用”} |

## 5. 边缘状态

| 场景 | 触发条件 | 系统行为 | 验收要点 |
|---|---|---|---|
| {场景} | {条件} | {行为} | {可判真伪的要点} |
