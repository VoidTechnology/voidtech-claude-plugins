# {模块名称} PRD

- **日期**：{YYYY-MM-DD}
- **状态**：Draft
- **深度**：{骨架级/验收级}
- **所属系统**：{系统名称}
- **所属模块**：{模块名称}
- **原始需求来源**：{`_source/` 内路径与章节}
- **引用领域规格**：{domain-specs 文件列表，无则写“无”}
- **摘要**：{一句话说明模块价值、范围和核心边界}

> 跨端复用的对象、状态机、字段规则已在领域规格定义的，第 6、7 节只写本端可见范围与操作差异并链接领域规格，不复制内容；本端独有的对象仍在本文件定义。期次口径以追溯矩阵为唯一权威。

## 1. 背景与问题

### 1.1 业务背景

{说明该模块为什么存在，服务哪个业务链路。}

### 1.2 当前问题

| 问题 | 影响对象 | 影响 |
|---|---|---|
| {问题} | {用户/角色/系统} | {影响} |

### 1.3 无系统时的绕行方式

{说明用户当前如何绕行，以及为什么绕行不可持续。}

## 2. 目标用户与使用场景

| 用户/角色 | 使用场景 | 目标 |
|---|---|---|
| {角色} | {场景} | {目标} |

## 3. 需求范围

### 3.1 本期包含

- {范围}

### 3.2 本期不做

- {不做事项与原因}

### 3.3 模块边界

| 边界项 | 本模块负责 | 不负责 | 依赖模块/系统 |
|---|---|---|---|
| {边界} | {负责内容} | {不负责内容} | {依赖} |

### 3.4 模块交互（机器可解析）

> Logic Atlas 编译器只消费本表（表头与列序固定，不得改动）；目标模块用工作树内的模块目录名。无跨模块交互写「无」并删除表格。

| 目标模块 | 方向 | 触发 | 失败传播 |
|---|---|---|---|
| {目录名} | {调用/被调用} | {触发场景} | {失败时用户可见结果} |

## 4. 功能清单

| 需求编号 | 原始需求 | 功能描述 | 入口 | 前置条件 | 用户操作 | 系统响应 | 异常处理 | 权限 | 数据 |
|---|---|---|---|---|---|---|---|---|---|
| {REQ-ID} | {原文摘要} | {功能} | {入口} | {条件} | {操作} | {响应} | {异常} | {权限} | {数据} |

## 5. 核心用户路径

### 5.0 页面契约（机器可解析）

> Logic Atlas 编译器只消费本表（表头与列序固定，不得改动）。同一页面可写多行（一行一个动作），编译器按页面名合并；无独立页面的纯服务模块写「无」并删除表格。

| 页面 | 入口 | 角色 | 前置条件 | 用户动作 | 系统结果 |
|---|---|---|---|---|---|
| {页面名} | {入口} | {角色} | {前置条件} | {动作} | {结果} |

### 5.0.1 核心流程（机器可解析）

> Logic Atlas 编译器只消费本表（表头与列序固定，不得改动）。一行一个行为步骤；`步骤ID` 在同一流程内唯一，`下一步` 填同流程步骤ID或「结束」；关联页面必须已在 §5.0 声明，同一步骤关联多个页面时用带空格的 ` / ` 分隔。失败处理不得合并进成功结果。需求编号可写多个，以 `/` 或 `、` 分隔。

| 流程 | 步骤ID | 关联页面 | 角色 | 用户动作/触发 | 条件/分支 | 系统结果 | 下一步 | 失败处理 | 需求编号 |
|---|---|---|---|---|---|---|---|---|---|
| {流程名称} | {S1} | {页面名} | {角色} | {动作或系统触发} | {进入条件或分支条件} | {成功结果} | {S2/结束} | {失败时用户可见结果与恢复路径} | {REQ-ID} |

### 5.1 {路径名称}

**正常流程**

1. {步骤}
2. {步骤}
3. {结果}

**边缘状态**

> 「页面」必须引用 §5.0 已声明页面；同一流程涉及多个页面时逐行明确归属，不按上下文推断。

| 页面 | 状态 | 触发条件 | 系统行为 | 用户可执行操作 | 验收要点 |
|---|---|---|---|---|---|
| {页面名} | 加载中 | {条件} | {行为} | {操作} | {验收} |
| {页面名} | 空数据 | {条件} | {行为} | {操作} | {验收} |
| {页面名} | 权限不足 | {条件} | {行为} | {操作} | {验收} |
| {页面名} | 重复提交 | {条件} | {行为} | {操作} | {验收} |
| {页面名} | 对象不存在 | {条件} | {行为} | {操作} | {验收} |
| {页面名} | 状态已变化 | {条件} | {行为} | {操作} | {验收} |

## 6. 状态机与状态流转

> 不涉及状态流转的模块，说明“不涉及”及原因。对象状态机已在领域规格定义时，此处写“引用 `{domain-spec 路径}`”，并只列本端可见状态与操作差异。

| 对象 | 当前状态 | 状态含义 | 进入条件 | 可执行操作 | 下一状态 | 是否可逆 | 操作人 | 通知/日志 |
|---|---|---|---|---|---|---|---|---|
| {对象} | {状态} | {含义} | {条件} | {操作} | {状态} | {是/否} | {角色} | {触发项} |

## 7. 字段与数据规则

### 7.0 数据读写（机器可解析）

> Logic Atlas 编译器只消费本表（表头与列序固定，不得改动）。操作只写「读」或「写」，读写皆有拆两行。

| 数据对象 | 操作 | 权威来源 | 同步方式 |
|---|---|---|---|
| {对象} | {读/写} | {模块或系统} | {实时/异步/导入} |

> 字段已在领域规格定义时，此处写“引用 `{domain-spec 路径}`”，并只列本端差异（可见性、可编辑性、脱敏）。

| 对象 | 字段 | 含义 | 类型 | 必填 | 示例 | 来源 | 校验规则 | 可编辑 | 可导出 | 敏感 |
|---|---|---|---|---|---|---|---|---|---|---|
| {对象} | {字段} | {含义} | {类型} | {是/否} | {示例} | {来源} | {规则} | {是/否} | {是/否} | {是/否} |

### 7.1 金额规则

- 币种：{规则}
- 精度：{规则}
- 四舍五入：{规则}
- 统计口径：{规则}

### 7.2 时间规则

- 时区：{规则}
- 起止边界：{规则}
- 归属口径：{规则}

## 8. 权限矩阵

> 本节管角色 × 操作。功能开通（租户类型/套餐档能否使用本模块）归 `00-global/feature-gating-matrix.md`，引用功能点 ID 即可，不在此重复。

| 角色 | 查看 | 新增 | 编辑 | 删除 | 审批 | 导入 | 导出 | 启用/关闭 | 查看敏感信息 | 查看日志 |
|---|---|---|---|---|---|---|---|---|---|---|
| {角色} | {规则} | {规则} | {规则} | {规则} | {规则} | {规则} | {规则} | {规则} | {规则} | {规则} |

## 9. 通知与消息

| 触发时机 | 接收人 | 渠道 | 文案变量 | 失败处理 | 可配置 | 日志 |
|---|---|---|---|---|---|---|
| {时机} | {接收人} | {渠道} | {变量} | {处理} | {是/否} | {记录内容} |

## 10. 埋点与统计口径

| 指标/事件 | 触发时机 | 统计口径 | 参数 | 用途 |
|---|---|---|---|---|
| {指标} | {时机} | {口径} | {参数} | {用途} |

## 11. 验收标准

> 验收级交付必须编号成表，覆盖正常、异常、权限、边界路径，每条可判真伪；不允许出现「样例」字样。骨架级允许先写代表性条目，但必须在深化任务清单登记补全任务。

| 编号 | 场景 | 前置条件 | 操作 | 预期结果 |
|---|---|---|---|---|
| AC-{序号} | {场景} | {条件} | {操作} | {可判真伪的结果} |

## 12. 开放问题

> 正文按推荐默认方案落笔处，用 `[推荐默认]` 标记并回指本表或全局清单的 OQ 编号。

| 编号 | 问题描述 | 影响范围 | 风险等级 | 推荐默认方案 | 需要确认人 |
|---|---|---|---|---|---|
| OQ-{序号} | {问题} | {范围} | 高/中/低 | {推荐方案，不能写成已确认} | {角色} |

## 13. 需求追溯

> 验收级交付本节逐条需求成行，不允许区间——包括引用领域规格承载的部分。全局追溯矩阵对本模块保留区间入口并指向本节。

| 需求编号 | 原始需求章节 | 原始需求点 | PRD 对应章节 | 覆盖状态 | 是否推断 | 是否有开放问题 |
|---|---|---|---|---|---|---|
| {REQ-ID} | {章节} | {原文} | {章节} | 已覆盖/部分覆盖/待确认 | 是/否 | 是/否 |

## 14. 变更记录

| 日期 | 版本 | 变更摘要 | 变更原因 | 影响范围 | 修改人 |
|---|---|---|---|---|---|
| {YYYY-MM-DD} | {版本} | {摘要} | {原因} | {范围} | {角色/姓名} |
