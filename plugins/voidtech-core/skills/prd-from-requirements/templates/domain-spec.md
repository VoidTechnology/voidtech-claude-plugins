# 领域规格：{领域名称}

- **日期**：{YYYY-MM-DD}
- **状态**：Draft
- **深度**：{骨架级/验收级}
- **覆盖需求**：{REQ-ID 区间或列表}
- **引用方**：{引用本规格的模块 PRD 列表}

> 跨端复用的业务对象、状态机、字段规则、边缘状态在此只定义一次；端/模块 PRD 只写本端可见范围与操作差异并引用本文件，不得复制。本端独有的对象仍在模块 PRD 内定义。

## 1. 对象

| 对象 | 一句话定义 | 来源需求 |
|---|---|---|
| {对象} | {定义} | {REQ-ID} |

## 2. 状态机

> 写法见 `references/state-machine-guide.md`。每条流转必须写清触发条件与触发方式；必须覆盖逆向流转和终态。到点自动流转与人工操作流转语义不同，不允许含糊。

```mermaid
stateDiagram-v2
    [*] --> {状态}
```

| 对象 | 当前状态 | 进入条件 | 可执行操作 | 下一状态 | 触发方式（人工/定时/事件） | 是否可逆 | 通知/日志 |
|---|---|---|---|---|---|---|---|
| {对象} | {状态} | {条件} | {操作} | {状态} | {方式} | {是/否} | {触发项} |

### 状态联动

> 对象之间有级联影响时（如「课程取消 → 未开始课时批量作废」），逐条写清影响范围与例外。

- {联动规则}

### 词表映射与裁决

> 原始需求在不同端对同一对象使用不同状态词表时，在此统一裁决，注明被废弃词表的来源需求编号；各端 PRD 不得另建状态词表。同一状态在全部文档中必须使用同一名称。

| 端/来源 | 原词表 | 统一后状态 | 裁决说明 |
|---|---|---|---|
| {端} | {原词表} | {状态} | {说明，含被废弃词表的 REQ-ID} |

## 3. 字段规则

| 字段 | 含义 | 类型 | 长度/格式 | 必填 | 校验规则 | 敏感 | 来源需求 |
|---|---|---|---|---|---|---|---|
| {字段} | {含义} | {类型} | {规则} | {是/否} | {规则} | {是/否} | {REQ-ID} |

## 4. 边缘状态

> 本节写业务逆向与并发类边缘（满额并发、重复提交、支付超时、资格过期、外部系统不可用等）；交互层边缘（加载中、空数据、弱网、会话过期）由引用方模块 PRD 负责，不在此处豁免。

| 场景 | 触发条件 | 系统行为 | 验收要点 |
|---|---|---|---|
| {场景} | {条件} | {行为} | {可判真伪的要点} |

## 5. 一期不做

- {排除项与原因}
