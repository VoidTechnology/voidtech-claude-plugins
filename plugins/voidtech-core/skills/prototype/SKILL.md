---
name: prototype
description: 构建一个用完即弃的原型来充实某个设计——可以是用于回答状态/业务逻辑问题的可运行终端应用，也可以是在同一路由上可切换的几种截然不同的 UI 变体。
disable-model-invocation: true
---
> Vendored from [mattpocock/skills](https://github.com/mattpocock/skills) · MIT © 2026 Matt Pocock · upstream 6eeb81b · 汉化:仅译用户可见文案,逻辑/结构未改。LICENSE 见 ../_vendor-licenses/mattpocock-LICENSE

# Prototype

原型是**用来回答某个问题的、用完即弃的代码**。问题决定了它的形态。

## 选择一个分支

确认要回答的是哪个问题——可以从用户的提示、周围的代码中判断，或者在用户在场时直接询问：

- **"这套逻辑/状态模型感觉对吗？"** → [LOGIC.md](LOGIC.md)。构建一个微型的交互式终端应用，把状态机推过那些在纸面上难以推演的情形。
- **"这东西应该长什么样？"** → [UI.md](UI.md)。在单个路由上生成几种截然不同的 UI 变体，通过 URL search param 和一个浮动的底部栏来切换。

两个分支产出的产物迥然不同——选错就会浪费掉整个原型。如果问题确实含糊不清且用户又联系不上，那就默认选择与周围代码更匹配的那个分支（后端模块 → 逻辑；页面或组件 → UI），并在原型顶部说明这个假设。

## 两个分支都适用的规则

1. **从第一天起就是用完即弃的，并明确标注为此。**把原型代码放在它实际会被用到的地方附近（紧挨着它在为之做原型的那个模块或页面），让上下文一目了然——但要给它取个名字，让随便看一眼的人也能看出这是原型而非生产代码。对于用完即弃的 UI 路由，遵循项目已有的路由约定；不要发明新的顶层结构。
2. **一条命令即可运行。**用项目现有 task runner 所支持的任何方式——`pnpm <name>`、`python <path>`、`bun <path>` 等等。用户必须能不假思索地启动它。
3. **默认不做持久化。**状态存活于内存中。持久化恰恰是原型要*检验*的东西，而不应是它所依赖的东西。如果问题明确涉及数据库，就连到一个临时的 scratch DB，或一个名字明确标着 "PROTOTYPE — wipe me" 的本地文件。
4. **跳过精修。**不写测试，不写超出"让原型能跑起来"所需的错误处理，不做抽象。重点是快速学到点什么然后删掉它。
5. **暴露状态。**每次动作之后（逻辑分支）或每次切换变体时（UI 分支），打印或渲染出全部相关状态，让用户能看到发生了什么变化。
6. **完事后删除或吸收。**当原型回答了它的问题，要么删掉它，要么把验证过的决策折叠进真正的代码——别让它烂在仓库里。

## 完成时

原型里唯一值得保留的就是**答案**。把它连同它所回答的问题一起，记录到某个持久的地方（commit message、ADR、issue，或紧挨着原型的 `NOTES.md`）。如果用户在场，这份记录就是一次简短的交谈；如果不在，就留下占位，好让他们（或下一轮的你）在删除原型之前填上结论。
