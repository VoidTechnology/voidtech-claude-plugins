# 安全政策

- 日期：2026-07-27
- 状态：Current
- 摘要：定义受支持版本、安全漏洞的私下报告渠道和响应边界。

## 受支持版本

项目只为各插件 `plugin.json` 声明的最新版本提供安全修复。历史版本不会单独维护；安全修复发布后，用户应更新 Marketplace 和对应插件。

`voidtech-loop` 当前只支持 README 声明的试点平台与最低 Claude Code 版本。

## 私下报告漏洞

请使用 GitHub 仓库的 **Security → Report a vulnerability** 私下提交安全报告。不要为以下问题创建公开 Issue、Discussion 或 Pull Request：

- 任意命令执行、Shell 注入或权限绕过；
- worktree、protected paths、Git refs 或安全门禁绕过；
- 凭据、Token、用户路径或敏感内容泄漏；
- Hook、MCP 或插件安装链路中的供应链问题；
- 可导致用户分支、文件或状态被未授权修改的问题。

报告请包含：

- 受影响插件和版本；
- 可复现步骤；
- 实际影响；
- 已知利用条件；
- 建议修复方向（可选）。

维护者会在 7 天内确认收到报告，并在完成影响评估后给出修复或披露计划。复杂问题的修复时间取决于影响范围和兼容风险。

## 披露原则

- 修复可用前不公开利用细节；
- 修复必须带回归测试；
- 发布说明必须写明受影响范围和升级方式；
- 凭据泄漏按已泄漏处理，立即撤销并轮换，不以删除 Git 文件替代轮换；
- 涉及第三方依赖时同步联系上游，但不向上游发送用户凭据或私有数据。

## 非安全问题

普通缺陷、文档错误和功能建议请使用 Issue 模板提交。
